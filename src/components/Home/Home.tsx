import React from 'react';
import { HomeContainer } from './Home.elements';

interface HomeProps {

}

const Home: React.FC<HomeProps> = ({}) => {
    return (
        <HomeContainer>
            
        </HomeContainer>
    );
}

export default Home;